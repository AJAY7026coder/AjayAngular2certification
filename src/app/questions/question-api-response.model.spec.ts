import { QuestionApiResponse } from './question-api-response.model';

describe('QuestionApiResponse', () => {
  it('should create an instance', () => {
    expect(new QuestionApiResponse()).toBeTruthy();
  });
});
