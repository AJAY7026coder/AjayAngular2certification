import { CategoryApiResponse } from './category-api-response.model';

describe('CategoryApiResponse', () => {
  it('should create an instance', () => {
    expect(new CategoryApiResponse()).toBeTruthy();
  });
});
